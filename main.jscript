function strip(html){
  var tmp = document.createElement("DIV");
  tmp.innerHTML = html;
  return tmp.textContent||tmp.innerText;
}

function getHTML(){
  return strip(document.body.innerHTML);
}

function getNth

getHTML().indexOf("\"unchanged\">")
